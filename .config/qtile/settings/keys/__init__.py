from settings.keys.applications import keys as application_keys
from settings.keys.system import keys as system_keys
from settings.keys.window import keys as window_keys

# The final list of keybindings
keys = system_keys + window_keys + application_keys
